strict-peer-dependencies=false
auto-install-peers=true

# NOTE: node-linker lives in pnpm-workspace.yaml (nodeLinker: hoisted), not here.
# pnpm 11 reads workspace-level linker config from pnpm-workspace.yaml only.
